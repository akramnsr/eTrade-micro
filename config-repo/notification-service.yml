spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:postgresql://etrade-pg-notification:5432/etrade_notification_db
    username: etrade_user
    password: etrade_pass
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://etrade-keycloak:8080/realms/etrade
          jwk-set-uri: http://etrade-keycloak:8080/realms/etrade/protocol/openid-connect/certs

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:etrade@portnet.ma}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false

server:
  port: 8084

notification:
  email:
    from: eTrade <etrade@portnet.ma>
    enabled: false   # mettre true quand SMTP configuré

eureka:
  client:
    service-url:
      defaultZone: http://eureka-admin:eureka-pass@etrade-discovery-service:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    ma.portnet: DEBUG
    org.springframework.mail: DEBUG
